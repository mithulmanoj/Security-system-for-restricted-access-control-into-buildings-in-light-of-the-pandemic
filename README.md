A FreeRTOS based System built using a Cypress PSoC-6 MCU
to authorize access into a building using a PIR sensor, thermistor, Capsense module, passkey authentication, Servo
motor and an MQTT notification mechanism, where the
servo motor gets triggered if access into the building has
been successfully granted
